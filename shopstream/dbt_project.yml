name: 'shopstream'
version: '1.0.0'
config-version: 2

profile: 'shopstream'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  shopstream:
    staging:
      +materialized: view
      +schema: silver_staging
    gold:
      +materialized: table
      +schema: gold